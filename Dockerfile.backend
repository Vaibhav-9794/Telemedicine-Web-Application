# --- BACKEND ---
FROM node:20-alpine AS backend
WORKDIR /app/backend
COPY backend/package*.json ./
RUN npm ci --omit=dev
COPY backend/ ./
RUN mkdir -p data public/uploads
EXPOSE 5000
CMD ["node", "server.js"]
